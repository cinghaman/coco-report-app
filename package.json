{
  "name": "coco-sales-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd coco-report-app && npm ci && npm run build",
    "start": "cd coco-report-app && npm run start",
    "dev": "cd coco-report-app && npm run dev",
    "install:app": "cd coco-report-app && npm install"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}
